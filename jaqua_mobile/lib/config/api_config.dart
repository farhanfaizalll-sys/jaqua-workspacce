/// Base URL of the jaqua_backend API.
///
/// TEMPORARY: pointed at the local dev backend (localhost:4000) for testing
/// before Render deployment. Once deployed, switch back to the real Render
/// URL, e.g. 'https://jaqua-backend.onrender.com' (Render assigns the exact
/// subdomain at service creation).
const String kApiBaseUrl = 'http://localhost:4000';
